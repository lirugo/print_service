directive @dateFormat on FIELD_DEFINITION

type Query {
    users: [User]
}

type User {
    name: String,
    email: String,
    picture: String,
    room: String,
    lastVisit: String @dateFormat,
    roles: [String],
}
